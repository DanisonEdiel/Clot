name: 🚀 Deploy Vue to EC2

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: 📂 Clonar repositorio en GitHub Actions
        uses: actions/checkout@v3

      - name: 🔄 Conectarse a EC2, clonar código y crear `.env`
        env:
          EC2_HOST: ${{ secrets.EC2_HOST }}
          BACKEND_HOST: ${{ secrets.BACKEND_HOST }}
          SSH_PRIVATE_KEY: ${{ secrets.EC2_SSH_KEY }}
        run: |
          echo "$SSH_PRIVATE_KEY" > private_key.pem
          chmod 600 private_key.pem
          ssh -o StrictHostKeyChecking=no -i private_key.pem ec2-user@$EC2_HOST << EOF
            cd /home/ec2-user
            if [ -d "vue-app" ]; then
              cd vue-app && git pull origin main
            else
              git clone https://github.com/DanisonEdiel/Clot-Front.git vue-app
            fi

            # Crear el archivo .env con la IP del backend
            echo "VITE_API_URI=\"http://$BACKEND_HOST/api\"" > /home/ec2-user/vue-app/.env
            
            # Moverse al proyecto
            cd /home/ec2-user/vue-app
            
            # Eliminar contenedor y build anterior si existen
            docker stop clot_front || true
            docker rm clot_front || true
            docker rmi clot_front || true

            # Construir la imagen Docker
            docker build -t clot_front .

            # Ejecutar el contenedor en el puerto 80
            docker run -d -p 80:80 --name clot_front clot_front
          EOF
          rm -f private_key.pem
